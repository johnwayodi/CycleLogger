<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_splash"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.johnwayodi.cyclelogger.SplashActivity">

    <TextView
        android:text="Cycle Logger"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="55dp"
        android:id="@+id/splash_text"
        android:textAppearance="@android:style/TextAppearance.Material.Large"
        android:textColor="@color/colorPrimaryDark"
        android:layout_alignBottom="@+id/splash_extra_text"
        android:layout_centerHorizontal="true"
        tools:targetApi="lollipop" />

    <TextView
        android:text="record your rides easily"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/splash_extra_text"
        android:textColor="@color/colorPrimary"
        android:layout_marginBottom="81dp"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true" />

    <ImageView
        android:layout_width="200dp"
        android:layout_height="200dp"
        app:srcCompat="@drawable/cycleloggera"
        android:id="@+id/splash_logo"
        android:scaleType="fitCenter"
        android:layout_marginBottom="21dp"
        android:layout_above="@+id/splash_text"
        android:layout_centerHorizontal="true" />

</RelativeLayout>
